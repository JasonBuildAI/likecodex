'use client';import { useCallback, useEffect, useRef, useState } from 'react';import { ChatMessages } from '@/components/Chat';import { DiffViewer } from '@/components/DiffViewer';import { PermissionModal } from '@/components/PermissionModal';import { AskModal } from '@/components/AskModal';import { CheckpointPanel } from '@/components/CheckpointPanel';import { SetupBanner } from '@/components/SetupBanner';import { TaskTimeline } from '@/components/TaskTimeline';import { SettingsPanel } from '@/components/SettingsPanel';import { CommandPalette } from '@/components/CommandPalette';import { CodeGraphSearch } from '@/components/CodeGraphSearch';import { SkillPanel } from '@/components/SkillPanel';import { FileTree } from '@/components/FileTree';import { EditorPanel } from '@/components/EditorPanel';import { StatusBar } from '@/components/StatusBar';import { MentionPicker } from '@/ide/context/MentionPicker';import type { ContextMention } from '@/ide/context/types';import { ComposerPanel } from '@/ide/composer/ComposerPanel';import { useComposerStore } from '@/ide/composer/composerStore';import { GitPanel } from '@/ide/git/GitPanel';import { SearchPanel } from '@/ide/search/SearchPanel';import { TerminalPanel } from '@/ide/terminal/TerminalPanel';import { TestRunnerPanel } from '@/ide/debug/TestRunnerPanel';import { DebugToolbar } from '@/ide/debug/DebugToolbar';import { IDESettingsPanel } from '@/ide/settings/IDESettingsPanel';import { ExtensionLoader } from '@/ide/extensions/extensionLoader';import { AgentSidebar } from '@/components/AgentSidebar';import { ShortcutHelpPanel } from '@/components/ShortcutHelp';import { OnboardingTooltips } from '@/components/Onboarding';import {  fetchCacheMetrics,  fetchConfig,  fetchDoctor,  fetchSessionEvents,  fetchSessions,  streamChat,  subscribeEvents,  createNewSession,} from '@/lib/api';import { useAppStore } from '@/lib/store';export default function Home() {  const [input, setInput] = useState('');  const [doctor, setDoctor] = useState<Awaited<ReturnType<typeof fetchDoctor>>>(null);  const [inputHistory, setInputHistory] = useState<string[]>([]);  const [historyIndex, setHistoryIndex] = useState(-1);  const [chatOpen, setChatOpen] = useState(true);  const [diffOpen, setDiffOpen] = useState(false);  const [terminalOpen, setTerminalOpen] = useState(false);  const [leftPanel, setLeftPanel] = useState<'files' | 'agents' | 'sessions' | 'search' | 'git' | 'tests' | 'skills'>('files');  const [debugOpen, setDebugOpen] = useState(false);  const [ideSettingsOpen, setIdeSettingsOpen] = useState(false);  const [showMentions, setShowMentions] = useState(false);  const [mentionQuery, setMentionQuery] = useState('');  const [mentionPos, setMentionPos] = useState({ top: 0, left: 0 });  const [mentions, setMentions] = useState<ContextMention[]>([]);  const abortRef = useRef<AbortController | null>(null);  const scrollRef = useRef<HTMLDivElement | null>(null);  const textareaRef = useRef<HTMLTextAreaElement | null>(null);  const messages = useAppStore((s) => s.messages);  const tasks = useAppStore((s) => s.tasks);  const planSteps = useAppStore((s) => s.planSteps);  const sessions = useAppStore((s) => s.sessions);  const pendingPermissions = useAppStore((s) => s.pendingPermissions);  const pendingAskRequests = useAppStore((s) => s.pendingAskRequests);  const activeDiff = useAppStore((s) => s.activeDiff);  const cacheHitRate = useAppStore((s) => s.cacheHitRate);  const planModeActive = useAppStore((s) => s.planModeActive);  const collaborationMode = useAppStore((s) => s.collaborationMode);  const isStreaming = useAppStore((s) => s.isStreaming);  const currentSessionId = useAppStore((s) => s.currentSessionId);  const openFiles = useAppStore((s) => s.openFiles);  const activeFilePath = useAppStore((s) => s.activeFilePath);  const composerOpen = useComposerStore((s) => s.isOpen);  const toggleComposer = useComposerStore((s) => s.toggleComposer);  const setCollaborationMode = useAppStore((s) => s.setCollaborationMode);  const setPlanMode = useAppStore((s) => s.setPlanMode);  const setCacheHitRate = useAppStore((s) => s.setCacheHitRate);  const addMessage = useAppStore((s) => s.addMessage);  const appendToLastMessage = useAppStore((s) => s.appendToLastMessage);  const upsertToolDispatch = useAppStore((s) => s.upsertToolDispatch);  const setIsStreaming = useAppStore((s) => s.setIsStreaming);  const setTasks = useAppStore((s) => s.setTasks);  const updateTask = useAppStore((s) => s.updateTask);  const setCurrentTaskId = useAppStore((s) => s.setCurrentTaskId);  const addPendingPermission = useAppStore((s) => s.addPendingPermission);  const removePendingPermission = useAppStore((s) => s.removePendingPermission);  const addPendingAsk = useAppStore((s) => s.addPendingAsk);  const removePendingAsk = useAppStore((s) => s.removePendingAsk);  const setPlanSteps = useAppStore((s) => s.setPlanSteps);  const updatePlanStep = useAppStore((s) => s.updatePlanStep);  const setActiveDiff = useAppStore((s) => s.setActiveDiff);  const setSessions = useAppStore((s) => s.setSessions);  const setConfig = useAppStore((s) => s.setConfig);  const setCurrentSessionId = useAppStore((s) => s.setCurrentSessionId);  const setMessages = useAppStore((s) => s.setMessages);  const addToast = useAppStore((s) => s.addToast);  const agentMode = useAppStore((s) => s.agentMode);  const setAgentMode = useAppStore((s) => s.setAgentMode);  // 鈹€鈹€ Load extensions on mount 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€  useEffect(() => {    ExtensionLoader.loadExtensions();  }, []);  // 鈹€鈹€ Initialization 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€  useEffect(() => {    fetchConfig().then(setConfig);    fetchSessions().then(setSessions);    fetchDoctor().then(setDoctor);    fetchCacheMetrics().then((metrics) => {      const rate = metrics.recent_hit_rate ?? metrics.hit_rate;      setCacheHitRate(typeof rate === 'number' ? rate : null);    });    const interval = setInterval(() => {      fetchCacheMetrics().then((metrics) => {        const rate = metrics.recent_hit_rate ?? metrics.hit_rate;        setCacheHitRate(typeof rate === 'number' ? rate : null);      });    }, 15000);    return () => clearInterval(interval);  }, [setConfig, setSessions, setCacheHitRate]);  // 鈹€鈹€ Event subscription 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€  useEffect(() => {    const unsubscribe = subscribeEvents({      onMessage: addMessage,      onAppend: appendToLastMessage,      onUpsertToolDispatch: upsertToolDispatch,      onPermission: addPendingPermission,      onPermissionResponded: (requestId) => removePendingPermission(requestId),      onAsk: addPendingAsk,      onAskResponded: (requestId) => removePendingAsk(requestId),      onPlanModeChanged: (active) => setPlanMode(active),      onPlanStep: (step) => {        const existing = useAppStore.getState().planSteps;        if (existing.find((s) => s.id === step.id)) {          updatePlanStep(step.id, step);        } else {          setPlanSteps([...existing, step]);        }      },      onDiff: (before, after) => {        setActiveDiff({ before, after });        setDiffOpen(true);      },      onError: (err) => {        addToast({ type: 'error', message: err.message });      },    });    return unsubscribe;  }, [    addMessage, appendToLastMessage, upsertToolDispatch,    addPendingPermission, removePendingPermission, addPendingAsk, removePendingAsk,    setPlanMode, setPlanSteps, updatePlanStep, setActiveDiff, addToast,  ]);  // 鈹€鈹€ Chat logic 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€  const runPrompt = useCallback(async (prompt: string) => {    if (!prompt.trim() || isStreaming) return;    setInputHistory((prev) => [prompt, ...prev].slice(0, 50));    setHistoryIndex(-1);    addMessage({      id: `user-${Date.now()}`,      role: 'user',      content: prompt,      timestamp: Date.now(),    });    setIsStreaming(true);    setPlanSteps([]);    abortRef.current?.abort();    abortRef.current = new AbortController();    // Get active files from store to inject into context    const { openFiles, activeFilePath } = useAppStore.getState();    const activeFiles = openFiles      .filter((f) => f.path === activeFilePath || f.modified)      .map((f) => f.path)      .slice(0, 5); // Limit to 5 files    try {      await streamChat(        prompt,        currentSessionId,        {          onMessage: addMessage,          onAppend: appendToLastMessage,          onUpsertToolDispatch: upsertToolDispatch,          onTaskStarted: (task) => {            setTasks([...useAppStore.getState().tasks, task]);            setCurrentTaskId(task.id);            if (!currentSessionId) setCurrentSessionId(task.id);          },          onTaskCompleted: (taskId, failed) => {            updateTask(taskId, { status: failed ? 'failed' : 'completed' });            fetchSessions().then(setSessions);          },          onStreamFinished: () => setIsStreaming(false),          onPermission: addPendingPermission,          onAsk: addPendingAsk,          onAskResponded: (requestId) => removePendingAsk(requestId),          onPlanModeChanged: (active) => setPlanMode(active),          onPlanStep: (step) => {            const existing = useAppStore.getState().planSteps;            if (existing.find((s) => s.id === step.id)) {              updatePlanStep(step.id, step);            } else {              setPlanSteps([...existing, step]);            }          },        },        abortRef.current.signal,        agentMode,        activeFiles      );    } catch (err) {      addMessage({        id: `error-${Date.now()}`,        role: 'system',        content: `Failed: ${err instanceof Error ? err.message : String(err)}`,        timestamp: Date.now(),      });      setIsStreaming(false);      addToast({ type: 'error', message: `Failed: ${err instanceof Error ? err.message : String(err)}` });    }  }, [    isStreaming, currentSessionId, setCurrentSessionId, addMessage, setIsStreaming, setPlanSteps,    appendToLastMessage, upsertToolDispatch, setTasks, setCurrentTaskId,    updateTask, setSessions, addPendingPermission, addPendingAsk, removePendingAsk,    setPlanMode, updatePlanStep, addToast, agentMode,  ]);  const handleSubmit = async (e: React.FormEvent) => {    e.preventDefault();    if (!input.trim() || isStreaming) return;    const prompt = input;    setInput('');    await runPrompt(prompt);  };  const handleKeyDown = (e: React.KeyboardEvent<HTMLTextAreaElement>) => {    if (e.key === 'Enter' && (e.ctrlKey || e.metaKey)) {      e.preventDefault();      if (!input.trim() || isStreaming) return;      const prompt = input;      setInput('');      runPrompt(prompt);      return;    }    if (e.key === 'Escape') {      useAppStore.getState().setCommandPaletteOpen(false);      useAppStore.getState().setSettingsOpen(false);      return;    }    if (e.key === 'ArrowUp' && input === '') {      e.preventDefault();      setHistoryIndex((prev) => {        const next = Math.min(prev + 1, inputHistory.length - 1);        if (inputHistory[next] !== undefined) setInput(inputHistory[next]);        return next;      });      return;    }    if (e.key === 'ArrowDown' && historyIndex >= 0) {      e.preventDefault();      setHistoryIndex((prev) => {        const next = prev - 1;        if (next < 0) {          setInput('');          return -1;        }        setInput(inputHistory[next] || '');        return next;      });    }  };  const handleInputChange = (e: React.ChangeEvent<HTMLTextAreaElement>) => {    const value = e.target.value;    setInput(value);    // @ mention detection    const cursor = e.target.selectionStart;    const beforeCursor = value.slice(0, cursor);    const atIndex = beforeCursor.lastIndexOf('@');    if (atIndex !== -1 && (atIndex === 0 || beforeCursor[atIndex - 1] === ' ' || beforeCursor[atIndex - 1] === '\n')) {      const query = beforeCursor.slice(atIndex + 1);      if (!query.includes(' ') && !query.includes('\n') && query.length <= 50) {        setShowMentions(true);        setMentionQuery(query);        // Calculate position near cursor        const rect = textareaRef.current?.getBoundingClientRect();        if (rect) {          setMentionPos({            top: rect.bottom - 60,            left: rect.left + 20,          });        }        return;      }    }    setShowMentions(false);  };  const handleMentionSelect = (mention: ContextMention) => {    const cursor = textareaRef.current?.selectionStart || input.length;    const beforeCursor = input.slice(0, cursor);    const atIndex = beforeCursor.lastIndexOf('@');    if (atIndex === -1) return;    const afterCursor = input.slice(cursor);    const mentionTag = `@[${mention.label}](${mention.id}) `;    const newInput = input.slice(0, atIndex) + mentionTag + afterCursor;    setInput(newInput);    setShowMentions(false);    // Focus back to textarea and position cursor after mention    setTimeout(() => {      if (textareaRef.current) {        const newCursor = atIndex + mentionTag.length;        textareaRef.current.focus();        textareaRef.current.setSelectionRange(newCursor, newCursor);      }    }, 0);  };  // 鈹€鈹€ Global keyboard shortcuts 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€  useEffect(() => {    const handler = (e: KeyboardEvent) => {      const isInput = document.activeElement?.tagName === 'INPUT' || document.activeElement?.tagName === 'TEXTAREA';      if (e.key === 'k' && (e.ctrlKey || e.metaKey) && !e.shiftKey) {        e.preventDefault();        useAppStore.getState().setCommandPaletteOpen(true);        return;      }      if (e.key === 'b' && (e.ctrlKey || e.metaKey)) {        e.preventDefault();        useAppStore.getState().toggleSidebar();        return;      }      if (e.key === 'p' && (e.ctrlKey || e.metaKey) && e.shiftKey) {        e.preventDefault();        setIdeSettingsOpen(true);        return;      }      if (e.key === ',' && (e.ctrlKey || e.metaKey)) {        e.preventDefault();        setIdeSettingsOpen(true);        return;      }      if (e.key === 'n' && (e.ctrlKey || e.metaKey) && !isInput) {        e.preventDefault();        createNewSession().then((r) => {          setCurrentSessionId(r.session_id);          setMessages([]);          addToast({ type: 'success', message: 'New session created' });          fetchSessions().then(setSessions);        });      }    };    window.addEventListener('keydown', handler);    return () => window.removeEventListener('keydown', handler);  }, [setCurrentSessionId, setMessages, setSessions, addToast]);  // 鈹€鈹€ Session handling 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€  const handleSessionSelect = async (sessionId: string) => {    setCurrentSessionId(sessionId);    setIsStreaming(false);    setPlanSteps([]);    const events = await fetchSessionEvents(sessionId);    setMessages(events);  };  const cacheLabel = cacheHitRate !== null ? `${Math.round(cacheHitRate * 100)}%` : '--';  const hasDiff = activeDiff?.before || activeDiff?.after;  // 鈹€鈹€ Render 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€  return (    <main className="flex h-screen flex-col bg-background">      {/* Header */}      <header className="border-b border-border px-3 py-1.5 flex items-center justify-between bg-surface shrink-0">        <div className="flex items-center gap-2">          <button            onClick={() => useAppStore.getState().toggleSidebar()}            className="p-1 rounded hover:bg-accent/10 transition-colors"            title="Toggle sidebar (Ctrl+B)"          >            <svg className="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />            </svg>          </button>          <h1 className="text-sm font-semibold">LikeCodex</h1>        </div>        <div className="flex items-center gap-1.5 text-xs text-muted">          {/* Left panel tabs */}          <div className="flex gap-0.5 mr-2">            {(['files', 'agents', 'sessions', 'search', 'git', 'tests', 'skills'] as const).map((tab) => (              <button                key={tab}                onClick={() => setLeftPanel(tab)}                className={`px-2 py-0.5 rounded text-[10px] transition-colors ${                  leftPanel === tab ? 'bg-primary/20 text-primary' : 'hover:bg-accent/10'                }`}              >                {tab === 'files' ? 'Files' : tab === 'agents' ? 'Agents' : tab === 'sessions' ? 'History' : tab === 'search' ? 'Search' : tab === 'git' ? 'Git' : tab === 'tests' ? 'Tests' : 'Skills'}              </button>            ))}          </div>          <span className="text-border mx-0.5">|</span>          {/* Plan mode badge */}          {planModeActive ? (            <span className="px-2 py-0.5 rounded bg-amber-500/20 text-amber-200 text-[10px] font-medium">PLAN</span>          ) : null}          {/* Collaboration mode */}          <select            className="bg-transparent border border-border rounded px-1.5 py-0.5 text-[10px]"            value={collaborationMode}            onChange={(e) => {              const mode = e.target.value as 'normal' | 'plan' | 'goal';              setCollaborationMode(mode);              if (mode === 'plan') runPrompt('/plan');              if (mode === 'goal') runPrompt('/goal Continue autonomously on the active task');              if (mode === 'normal') runPrompt('/exit_plan');            }}          >            <option value="normal">normal</option>            <option value="plan">plan</option>            <option value="goal">goal</option>          </select>          {/* Chat toggle */}          <button            onClick={() => setChatOpen(!chatOpen)}            className={`px-1.5 py-0.5 rounded border text-[10px] transition-colors ${chatOpen ? 'border-primary text-primary' : 'border-border'}`}            title="Toggle chat panel"          >            Chat          </button>          {/* Terminal toggle */}          <button            onClick={() => setTerminalOpen(!terminalOpen)}            className={`px-1.5 py-0.5 rounded border text-[10px] transition-colors ${terminalOpen ? 'border-primary text-primary' : 'border-border'}`}            title="Toggle terminal (Ctrl+J)"          >            Terminal          </button>          {/* Debug toggle */}          <button            onClick={() => setDebugOpen(!debugOpen)}            className={`px-1.5 py-0.5 rounded border text-[10px] transition-colors ${debugOpen ? 'border-primary text-primary' : 'border-border'}`}            title="Toggle debug toolbar"          >            Debug          </button>          {/* Command palette */}          <button            onClick={() => useAppStore.getState().setCommandPaletteOpen(true)}            className="px-1.5 py-0.5 rounded border border-border text-[10px] hover:bg-accent/10 transition-colors"            title="Command palette (Ctrl+K)"          >            Cmd          </button>          <span className="text-border mx-0.5">|</span>          <span className="flex items-center gap-1 text-[10px]" title="Cache hit rate">            <svg className="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />            </svg>            {cacheLabel}          </span>        </div>      </header>      <SetupBanner doctor={doctor} />      {/* Main content: 3-column layout */}      <div className="flex flex-1 min-h-0">        {/* Left Panel: File tree / Sessions / Search / Skills */}        <aside className="w-56 border-r border-border bg-surface/30 overflow-y-auto shrink-0 flex flex-col">          {leftPanel === 'files' ? (            <FileTree />          ) : leftPanel === 'agents' ? (            <AgentSidebar              sessions={sessions}              tasks={tasks}              activeSessionId={currentSessionId}              onSessionSelect={handleSessionSelect}            />          ) : leftPanel === 'sessions' ? (            <div className="p-2 overflow-y-auto">              <div className="text-[10px] font-semibold uppercase tracking-wider text-muted/60 mb-2 px-1">                History              </div>              <TaskTimeline                tasks={tasks}                planSteps={planSteps}                sessions={sessions}                activeSessionId={currentSessionId}                onSessionSelect={handleSessionSelect}              />              <div className="mt-3">                <CheckpointPanel />              </div>            </div>          ) : leftPanel === 'search' ? (            <SearchPanel />          ) : leftPanel === 'git' ? (            <GitPanel />          ) : leftPanel === 'tests' ? (            <TestRunnerPanel />          ) : (            <SkillPanel />          )}        </aside>        {/* Center: Editor + Diff */}        <section className="flex-1 flex flex-col min-w-0">          {/* Debug toolbar */}          {debugOpen && <DebugToolbar />}          {/* Editor */}          <div className={`flex-1 min-h-0 ${diffOpen && hasDiff ? '' : 'flex-1'}`}>            <EditorPanel />          </div>          {/* Diff panel (collapsible bottom) */}          {diffOpen && hasDiff && (            <div className="h-1/3 min-h-[120px] border-t border-border flex flex-col">              <div className="flex items-center justify-between px-3 py-1 bg-surface/50 shrink-0">                <span className="text-[10px] font-semibold text-muted/60 uppercase tracking-wider">                  Changes                </span>                <button                  onClick={() => setDiffOpen(false)}                  className="text-[10px] text-muted hover:text-foreground"                >                  Close                </button>              </div>              <div className="flex-1 min-h-0">                <DiffViewer before={activeDiff?.before} after={activeDiff?.after} />              </div>            </div>          )}          {/* Terminal panel (collapsible bottom) */}          {terminalOpen && (            <div className="h-1/3 min-h-[120px] border-t border-border flex flex-col">              <div className="flex items-center justify-between px-3 py-1 bg-surface/50 shrink-0">                <span className="text-[10px] font-semibold text-muted/60 uppercase tracking-wider">                  Terminal                </span>                <button                  onClick={() => setTerminalOpen(false)}                  className="text-[10px] text-muted hover:text-foreground"                >                  Close                </button>              </div>              <div className="flex-1 min-h-0">                <TerminalPanel />              </div>            </div>          )}        </section>        {/* Right Panel: Chat - Redesigned to match Cursor */}        {chatOpen && (          <aside className="w-[480px] border-l border-border bg-surface flex flex-col shrink-0 relative">            {/* Top toolbar - Model selector & workspace */}            <div className="flex items-center justify-between px-4 py-2 border-b border-border bg-surface/50 shrink-0">              <div className="flex items-center gap-2">                {/* Workspace selector */}                <button className="flex items-center gap-1.5 text-xs text-muted hover:text-foreground transition-colors px-2 py-1 rounded hover:bg-accent/10">                  <span>{currentSessionId ? 'likecodex' : 'New Agent'}</span>                  <svg className="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />                  </svg>                </button>                                {/* Local/Remote indicator */}                <button className="flex items-center gap-1.5 text-xs text-muted hover:text-foreground transition-colors px-2 py-1 rounded hover:bg-accent/10">                  <svg className="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />                  </svg>                  <span>Local</span>                  <svg className="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />                  </svg>                </button>              </div>                            <div className="flex items-center gap-1.5">                {/* Cache indicator */}                <span className="flex items-center gap-1 text-[10px] text-muted/60" title="Cache hit rate">                  <svg className="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />                  </svg>                  {cacheLabel}                </span>                                {/* Keyboard shortcuts help */}                <ShortcutHelpPanel />                                {/* Settings button */}                <button                  onClick={() => setIdeSettingsOpen(true)}                  className="p-1.5 rounded hover:bg-accent/10 text-muted hover:text-foreground transition-colors"                  title="Settings (Ctrl+,)"                >                  <svg className="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />                  </svg>                </button>              </div>            </div>            {/* Chat messages area */}            <div ref={scrollRef} className="flex-1 overflow-y-auto p-4">              <ChatMessages scrollRef={scrollRef} />            </div>            {/* Centered Input Area - Cursor-style */}            <div className="absolute bottom-0 left-0 right-0 p-4 bg-gradient-to-t from-surface via-surface to-transparent">              <form onSubmit={handleSubmit} className="max-w-2xl mx-auto">                {/* Mode Selector - Modern Capsule Design */}                <div className="flex items-center justify-center mb-3">                  <div className="inline-flex items-center gap-1 bg-background/80 backdrop-blur-sm border border-border rounded-full px-1.5 py-1 shadow-lg">                    {(['ask', 'agent', 'manual'] as const).map((mode) => {                      const isActive = agentMode === mode;                      const colors = {                        ask: isActive ? 'bg-emerald-500/20 text-emerald-400 border-emerald-500/30' : '',                        agent: isActive ? 'bg-blue-500/20 text-blue-400 border-blue-500/30' : '',                        manual: isActive ? 'bg-amber-500/20 text-amber-400 border-amber-500/30' : '',                      };                      const icons = {                        ask: (                          <svg className="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />                          </svg>                        ),                        agent: (                          <svg className="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />                          </svg>                        ),                        manual: (                          <svg className="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 15l-2 5L9 9l11 4-5 2zm0 0l5 5M7.188 2.239l.777 2.897M5.136 7.965l-2.898-.777M13.95 4.05l-2.122 2.122m-5.657 5.656l-2.12 2.122" />                          </svg>                        ),                      };                      const labels = {                        ask: 'Ask',                        agent: 'Agent',                        manual: 'Manual',                      };                      const tooltips = {                        ask: 'Ask mode: read-only Q&A, no code changes',                        agent: 'Agent mode: autonomous execution with full tool access',                        manual: 'Manual mode: confirm each action before execution',                      };                      return (                        <button                          key={mode}                          type="button"                          onClick={() => setAgentMode(mode)}                          title={tooltips[mode]}                          className={`flex items-center gap-1.5 px-3 py-1.5 rounded-full text-xs font-medium transition-all ${                            isActive                              ? `${colors[mode]} shadow-md`                              : 'text-muted hover:text-foreground hover:bg-accent/10'                          }`}                        >                          {icons[mode]}                          <span>{labels[mode]}</span>                        </button>                      );                    })}                  </div>                </div>                {/* Main Input Box - Centered, Large */}                <div className="relative group">                  <div className="absolute inset-0 bg-gradient-to-r from-blue-500/20 via-purple-500/20 to-pink-500/20 rounded-2xl blur-xl opacity-0 group-hover:opacity-100 transition-opacity duration-500" />                  <div className="relative bg-background/90 backdrop-blur-sm border border-border rounded-2xl shadow-2xl overflow-hidden">                    {/* Input field */}                    <textarea                      ref={textareaRef}                      value={input}                      onChange={handleInputChange}                      onKeyDown={handleKeyDown}                      placeholder={                        agentMode === 'ask'                          ? 'Ask questions without making changes...'                          : agentMode === 'manual'                            ? 'Describe your task (each step requires approval)...'                            : 'What would you like to build? Use @ to reference files'                      }                      className="w-full bg-transparent px-4 py-3.5 pr-24 text-sm focus:outline-none resize-none min-h-[56px] max-h-[200px] placeholder:text-muted/60"                      rows={1}                      disabled={isStreaming}                    />                                        {/* Action buttons row */}                    <div className="flex items-center justify-between px-3 pb-2">                      <div className="flex items-center gap-2">                        {/* Add context button */}                        <button                          type="button"                          className="p-1.5 rounded-full hover:bg-accent/10 text-muted hover:text-foreground transition-colors"                          title="Add context (@files, #issues, etc.)"                        >                          <svg className="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 6v6m0 0v6m0-6h6m-6 0H6" />                          </svg>                        </button>                                                {/* File attachment indicator */}                        {activeFilePath && (                          <div className="flex items-center gap-1.5 px-2 py-1 rounded-full bg-primary/10 text-primary text-xs">                            <svg className="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">                              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />                            </svg>                            <span className="truncate max-w-[120px]">{activeFilePath.split('/').pop()}</span>                          </div>                        )}                      </div>                                            <div className="flex items-center gap-2">                        {/* Voice input button */}                        <button                          type="button"                          className="p-2 rounded-full hover:bg-accent/10 text-muted hover:text-foreground transition-colors"                          title="Voice input (coming soon)"                        >                          <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 11a7 7 0 01-7 7m0 0a7 7 0 01-7-7m7 7v4m0 0H8m4 0h4m-4-8a3 3 0 01-3-3V5a3 3 0 116 0v6a3 3 0 01-3 3z" />                          </svg>                        </button>                                                {/* Send button */}                        <button                          type="submit"                          disabled={isStreaming || !input.trim()}                          className={`p-2.5 rounded-full text-white shadow-lg transition-all transform hover:scale-105 active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100 ${                            agentMode === 'ask'                              ? 'bg-emerald-600 hover:bg-emerald-700'                              : agentMode === 'manual'                                ? 'bg-amber-600 hover:bg-amber-700'                                : 'bg-blue-600 hover:bg-blue-700'                          }`}                        >                          {isStreaming ? (                            <svg className="h-5 w-5 animate-spin" fill="none" viewBox="0 0 24 24">                              <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />                              <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />                            </svg>                          ) : (                            <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">                              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />                            </svg>                          )}                        </button>                      </div>                    </div>                  </div>                </div>                {/* Quick actions hint */}                {!isStreaming && (                  <div className="mt-3 text-center">                    <button                      type="button"                      onClick={() => setInput('/plan ')}                      className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full border border-border bg-background/50 hover:bg-accent/10 text-xs text-muted hover:text-foreground transition-colors"                    >                      <svg className="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />                      </svg>                      Plan New Idea                      <kbd className="ml-1 px-1.5 py-0.5 rounded bg-accent/20 text-[10px]">鈫慣ab</kbd>                    </button>                  </div>                )}                {/* Bottom hint text */}                <div className="mt-2 text-center text-[11px] text-muted/50">                  Use <kbd className="px-1.5 py-0.5 rounded bg-accent/10 text-[10px]">/model</kbd> to pick the best model for your task and select Composer for a great balance of cost vs. capability                </div>              </form>            </div>          </aside>        )}      </div>      {/* Composer Panel */}      <ComposerPanel />      {/* Composer toggle button */}      {!composerOpen && (        <button          onClick={toggleComposer}          className="fixed bottom-12 right-4 z-50 px-3 py-1.5 bg-blue-600 text-white text-xs rounded-full shadow-lg hover:bg-blue-700 transition-colors"          title="Open Composer (Cmd+I)"        >          鉁?Composer        </button>      )}      {/* @ Mention Picker */}      {showMentions && (        <MentionPicker          triggerPosition={mentionPos}          query={mentionQuery}          onSelect={handleMentionSelect}          onClose={() => setShowMentions(false)}        />      )}      {/* IDE Settings Panel */}      <IDESettingsPanel open={ideSettingsOpen} onClose={() => setIdeSettingsOpen(false)} />      {/* Status bar */}      <StatusBar />      {/* Overlays */}      <PermissionModal requests={pendingPermissions} onResponded={removePendingPermission} />      <AskModal requests={pendingAskRequests} onResponded={removePendingAsk} />      <SettingsPanel />      <CommandPalette />            {/* Onboarding tooltips for new users */}      <OnboardingTooltips />    </main>  );}