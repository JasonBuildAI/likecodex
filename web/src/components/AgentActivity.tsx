'use client';import { memo, useState } from 'react';import type { Message, ToolCall } from '@/lib/store';// 鈹€鈹€ Tool icon & label mapping 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€const TOOL_META: Record<string, { icon: string; label: string; color: string }> = {  read_file: { icon: '馃搫', label: 'Read file', color: 'text-blue-400' },  write_file: { icon: '鉁忥笍', label: 'Write file', color: 'text-green-400' },  edit_file: { icon: '鉁忥笍', label: 'Edit file', color: 'text-green-400' },  replace_in_file: { icon: '馃攧', label: 'Replace in file', color: 'text-green-400' },  run_command: { icon: '鈿?, label: 'Run command', color: 'text-amber-400' },  execute_command: { icon: '鈿?, label: 'Run command', color: 'text-amber-400' },  shell: { icon: '鈿?, label: 'Shell', color: 'text-amber-400' },  grep_search: { icon: '馃攳', label: 'Search', color: 'text-purple-400' },  codebase_search: { icon: '馃攳', label: 'Search code', color: 'text-purple-400' },  file_search: { icon: '馃攳', label: 'Find file', color: 'text-purple-400' },  list_dir: { icon: '馃搧', label: 'List directory', color: 'text-blue-400' },  create_file: { icon: '鉃?, label: 'Create file', color: 'text-green-400' },  delete_file: { icon: '馃棏锔?, label: 'Delete file', color: 'text-red-400' },  git_diff: { icon: '馃搳', label: 'Git diff', color: 'text-cyan-400' },  git_log: { icon: '馃摐', label: 'Git log', color: 'text-cyan-400' },  git_status: { icon: '馃搳', label: 'Git status', color: 'text-cyan-400' },  web_search: { icon: '馃寪', label: 'Web search', color: 'text-indigo-400' },  web_fetch: { icon: '馃寪', label: 'Fetch URL', color: 'text-indigo-400' },  lsp_definition: { icon: '馃敆', label: 'Go to definition', color: 'text-blue-400' },  lsp_references: { icon: '馃敆', label: 'Find references', color: 'text-blue-400' },  lsp_hover: { icon: '馃挕', label: 'Hover info', color: 'text-blue-400' },};const DEFAULT_META = { icon: '馃敡', label: 'Tool', color: 'text-muted' };function getToolMeta(name: string) {  return TOOL_META[name] || DEFAULT_META;}function getToolDescription(call: ToolCall): string {  const args = call.arguments || {};  switch (call.name) {    case 'read_file':    case 'write_file':    case 'edit_file':    case 'replace_in_file':    case 'create_file':    case 'delete_file':      return String(args.path || args.file_path || args.file || '');    case 'run_command':    case 'execute_command':    case 'shell':      return String(args.command || args.cmd || '').slice(0, 80);    case 'grep_search':    case 'codebase_search':      return String(args.pattern || args.query || args.search || '').slice(0, 60);    case 'file_search':      return String(args.pattern || args.query || '').slice(0, 60);    case 'list_dir':      return String(args.path || args.dir || '.');    case 'web_search':      return String(args.query || '').slice(0, 60);    case 'web_fetch':      return String(args.url || '').slice(0, 60);    case 'lsp_definition':    case 'lsp_references':    case 'lsp_hover':      return `${String(args.file_path || '')}:${String(args.line || '')} ${String(args.symbol || '')}`;    default:      return '';  }}// 鈹€鈹€ Single activity item 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€export const ActivityItem = memo(function ActivityItem({  call,  isRunning,  result,}: {  call: ToolCall;  isRunning: boolean;  result?: string;}) {  const [expanded, setExpanded] = useState(false);  const meta = getToolMeta(call.name);  const desc = getToolDescription(call);  const hasResult = result !== undefined;  return (    <div className="group">      <button        onClick={() => setExpanded(!expanded)}        className="flex items-center gap-2 w-full text-left py-0.5 px-1 rounded hover:bg-accent/5 transition-colors"      >        {/* Status indicator */}        <span className="shrink-0">          {isRunning ? (            <span className="inline-block h-3 w-3 rounded-full border-2 border-blue-400 border-t-transparent animate-spin" />          ) : hasResult ? (            <span className="inline-block h-3 w-3 rounded-full bg-green-500/80" />          ) : (            <span className="text-xs">{meta.icon}</span>          )}        </span>        {/* Tool label */}        <span className={`text-[11px] font-medium shrink-0 ${meta.color}`}>          {meta.label}        </span>        {/* Description */}        {desc && (          <span className="text-[11px] text-muted truncate ml-1">            {desc}          </span>        )}        {/* Expand indicator */}        {hasResult && (          <span className="ml-auto text-[9px] text-muted/40 shrink-0">            {expanded ? '鈻? : '鈻?}          </span>        )}      </button>      {/* Expanded result */}      {expanded && hasResult && (        <div className="ml-5 mt-1 mb-2 p-2 rounded bg-background/50 border border-border/50 max-h-48 overflow-auto">          <pre className="text-[10px] text-muted whitespace-pre-wrap break-all">{result}</pre>        </div>      )}      {/* Expanded arguments (when no result yet) */}      {expanded && !hasResult && (        <div className="ml-5 mt-1 mb-2 p-2 rounded bg-background/50 border border-border/50 max-h-32 overflow-auto">          <pre className="text-[10px] text-muted whitespace-pre-wrap break-all">            {JSON.stringify(call.arguments, null, 2)}          </pre>        </div>      )}    </div>  );});// 鈹€鈹€ Activity stream: groups consecutive tool messages 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€export interface ActivityEntry {  call: ToolCall;  isRunning: boolean;  result?: string;}export function extractActivities(messages: Message[]): ActivityEntry[] {  const activities: ActivityEntry[] = [];  const resultMap = new Map<string, string>();  // Build result map from tool_result messages  for (const msg of messages) {    if (msg.eventType === 'tool_result' && msg.toolCalls?.[0]) {      const key = msg.toolCalls[0].id || msg.toolCalls[0].name;      resultMap.set(key, msg.content);    }  }  // Collect tool calls  for (const msg of messages) {    if ((msg.eventType === 'tool_call' || msg.eventType === 'tool_dispatch') && msg.toolCalls?.[0]) {      const call = msg.toolCalls[0];      const key = call.id || call.name;      const isRunning = msg.eventType === 'tool_dispatch' || call.arguments?.partial === true;      activities.push({        call,        isRunning,        result: resultMap.get(key),      });    }  }  return activities;}// 鈹€鈹€ AgentActivity panel (renders inline in chat) 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€export const AgentActivity = memo(function AgentActivity({  activities,}: {  activities: ActivityEntry[];}) {  if (activities.length === 0) return null;  const runningCount = activities.filter((a) => a.isRunning).length;  const completedCount = activities.filter((a) => a.result !== undefined).length;  const totalCount = activities.length;  const progressPercent = totalCount > 0 ? Math.round((completedCount / totalCount) * 100) : 0;  return (    <div className="my-3 rounded-xl border border-border/60 bg-surface/40 overflow-hidden shadow-lg">      {/* Header with progress bar */}      <div className="px-4 py-2.5 border-b border-border/40 bg-surface/60">        <div className="flex items-center justify-between mb-2">          <div className="flex items-center gap-2">            {runningCount > 0 ? (              <svg className="h-4 w-4 text-blue-400 animate-spin" fill="none" viewBox="0 0 24 24">                <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />                <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />              </svg>            ) : (              <svg className="h-4 w-4 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />              </svg>            )}            <span className="text-xs font-semibold text-foreground">Agent Activity</span>          </div>          <div className="flex items-center gap-2">            <span className="text-[10px] text-muted">              {runningCount > 0 ? (                <span className="flex items-center gap-1 text-blue-400">                  <span className="inline-block h-1.5 w-1.5 rounded-full bg-blue-400 animate-pulse" />                  {runningCount} running                </span>              ) : (                <span className="text-green-400">鉁?{completedCount} completed</span>              )}            </span>            <span className="text-[10px] text-muted/60">{progressPercent}%</span>          </div>        </div>                {/* Progress bar */}        <div className="w-full h-1.5 bg-background rounded-full overflow-hidden">          <div             className={`h-full transition-all duration-500 ease-out ${              runningCount > 0 ? 'bg-gradient-to-r from-blue-500 to-purple-500' : 'bg-green-500'            }`}            style={{ width: `${progressPercent}%` }}          />        </div>      </div>            {/* Activity list */}      <div className="px-3 py-2 max-h-72 overflow-y-auto space-y-0.5">        {activities.map((activity, i) => (          <ActivityItem            key={activity.call.id || activity.call.name || i}            call={activity.call}            isRunning={activity.isRunning}            result={activity.result}          />        ))}      </div>    </div>  );});