'use client';import { memo, useEffect, useState } from 'react';// 鈹€鈹€ OnboardingTooltip: guided tour for new users 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€interface TooltipStep {  id: string;  title: string;  description: string;  position: { top: number; left: number };  highlight?: boolean;}export const OnboardingTooltips = memo(function OnboardingTooltips() {  const [currentStep, setCurrentStep] = useState(0);  const [isVisible, setIsVisible] = useState(false);  const [hasSeenOnboarding, setHasSeenOnboarding] = useState(false);  // Check if user has seen onboarding before  useEffect(() => {    const seen = localStorage.getItem('likecodex-onboarding-seen');    if (!seen) {      // Show onboarding after a short delay      const timer = setTimeout(() => {        setIsVisible(true);      }, 2000);      return () => clearTimeout(timer);    } else {      setHasSeenOnboarding(true);    }  }, []);  const steps: TooltipStep[] = [    {      id: 'mode-selector',      title: 'Choose Your Mode',      description: 'Select between Ask (read-only), Agent (autonomous), or Manual (confirm each step) mode.',      position: { top: window.innerHeight - 300, left: window.innerWidth / 2 - 150 },    },    {      id: 'input-box',      title: 'Describe Your Task',      description: 'Type what you want to build. Use @ to reference files and folders.',      position: { top: window.innerHeight - 200, left: window.innerWidth / 2 - 200 },    },    {      id: 'shortcuts',      title: 'Keyboard Shortcuts',      description: 'Press Ctrl+K for commands, Ctrl+B to toggle sidebar, and ? for help.',      position: { top: 60, left: window.innerWidth - 200 },    },  ];  const handleNext = () => {    if (currentStep < steps.length - 1) {      setCurrentStep((prev) => prev + 1);    } else {      // Finish onboarding      setIsVisible(false);      localStorage.setItem('likecodex-onboarding-seen', 'true');    }  };  const handleSkip = () => {    setIsVisible(false);    localStorage.setItem('likecodex-onboarding-seen', 'true');  };  if (!isVisible || hasSeenOnboarding) return null;  const currentTooltip = steps[currentStep];  return (    <>      {/* Backdrop */}      <div className="fixed inset-0 z-[90] bg-black/40 backdrop-blur-sm" />      {/* Tooltip */}      <div        className="fixed z-[95] max-w-xs animate-in fade-in slide-in-from-bottom-2"        style={{          top: currentTooltip.position.top,          left: currentTooltip.position.left,        }}      >        <div className="bg-surface border border-border rounded-xl shadow-2xl p-4">          {/* Header */}          <div className="flex items-center justify-between mb-2">            <h3 className="text-sm font-semibold text-foreground">              {currentTooltip.title}            </h3>            <span className="text-[10px] text-muted/60">              {currentStep + 1} / {steps.length}            </span>          </div>          {/* Description */}          <p className="text-xs text-muted leading-relaxed mb-3">            {currentTooltip.description}          </p>          {/* Actions */}          <div className="flex items-center justify-between">            <button              onClick={handleSkip}              className="text-[10px] text-muted hover:text-foreground transition-colors"            >              Skip tour            </button>            <button              onClick={handleNext}              className="px-3 py-1.5 bg-blue-600 text-white text-xs rounded-lg hover:bg-blue-700 transition-colors shadow-md"            >              {currentStep < steps.length - 1 ? 'Next' : 'Get Started'}            </button>          </div>        </div>        {/* Arrow pointer */}        <div className="absolute -bottom-2 left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-8 border-r-8 border-t-8 border-transparent border-t-surface" />      </div>    </>  );});// 鈹€鈹€ QuickTip: small contextual tips that appear near UI elements 鈹€鈹€鈹€鈹€鈹€鈹€鈹€interface QuickTipProps {  message: string;  visible: boolean;  onClose: () => void;  position: { top: number; left: number };}export const QuickTip = memo(function QuickTip({   message,   visible,   onClose,  position }: QuickTipProps) {  if (!visible) return null;  return (    <div      className="fixed z-[80] animate-in fade-in slide-in-from-bottom-2"      style={{ top: position.top, left: position.left }}    >      <div className="bg-surface border border-border rounded-lg shadow-xl px-3 py-2 flex items-center gap-2">        <svg className="h-3.5 w-3.5 text-blue-400 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />        </svg>        <span className="text-xs text-foreground">{message}</span>        <button          onClick={onClose}          className="ml-2 p-0.5 rounded hover:bg-accent/10 text-muted hover:text-foreground transition-colors"        >          <svg className="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />          </svg>        </button>      </div>    </div>  );});