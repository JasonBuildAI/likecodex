# Release notes — Surpass Reasonix milestone

## v0.7.0 (planned)

- Cache-First Context v2 (ImmutablePrefix / AppendOnlyLog / VolatileScratch)
- Tool-call repair pipeline + `edit_file` SEARCH/REPLACE
- Terminal: `likecodex code`, `doctor`, `stats`, `sessions`, engine auto-start
- Skills + Hooks + planner/subagent session isolation
- Persistent file index + web search + typed memory
- Public cache benchmark + security documentation

Install:

```bash
curl -fsSL https://raw.githubusercontent.com/JasonBuildAI/likecodex/main/scripts/install.sh | bash
likecodex doctor --security
likecodex code
```

Pre-built binaries: see GitHub Releases (Windows/macOS/Linux).
